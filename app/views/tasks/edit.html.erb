<h1>タスク編集画面</h1>
<p><%= @task.content %></p>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  <%= f.submit '更新 '%>
<% end %>
<%= link_to '一覧に戻る', tasks_path %>